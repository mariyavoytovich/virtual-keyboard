import { LANGUAGE } from "./Language";
import { KEY_CASE } from "./KeyCase";

export const KEYBOARD_SETTINGS = {
  languages: [LANGUAGE.ENG, LANGUAGE.RU],
  defaultLanguage: LANGUAGE.ENG,
  defaultKeyCase: KEY_CASE.DOWN,
}